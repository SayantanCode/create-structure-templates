# {{projectName}}

React + Vite frontend, composed from the pieces you chose when scaffolding.

## Project Structure

- `src/main.tsx` — mounts the app (wrapped in `ErrorBoundary`).
- `src/App.tsx` — where any selected providers (router, auth, ...) wrap the tree.
- `src/components/` — `Button`, `Input`, `Modal`, `Spinner`, `ErrorBoundary` — small, reusable, unopinionated components to build on.

<!-- __COMPOSER_README__ -->

## Getting started

```bash
cp .env.example .env
npm install
npm run dev
```
